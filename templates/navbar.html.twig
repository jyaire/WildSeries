<nav class="navbar navbar-expand-lg navbar-light bg-navbar">
    <a class="navbar-brand" href="{{ path('wild_index') }}">Wild Series</a>
    <a class="nav-link active" href="{{ path('wild_index') }}">Accueil</a>
    <a class="nav-link" href="{{ path('program_index') }}">Toutes les séries</a>
    <a class="nav-link" href="{{ path('season_index') }}">Toutes les saisons</a>
    <a class="nav-link" href="{{ path('episode_index') }}">Tous les épisodes</a>
    <a class="nav-link" href="{{ path('add_category') }}">Toutes les catégories</a>
</nav>